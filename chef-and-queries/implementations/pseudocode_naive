input N Q;
create array A[N];
for N:
    input A[i];

for Q:
    input type;
    if type == 1:
        input L R X;
        for i in L..R:
            A[i] += (X + i - L)^2;

    if type == 2:
        input Y;
        print A[Y];
